spring:
  datasource:
    url: jdbc:mysql://localhost:3306/VolunteerInfo?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: hello
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
